import { useState, useEffect } from 'react'

function EnterScores() {

    //Scorecard will be generated based on the course selected on round creation - this page will need to save data as it goes? So an unfinished round can be reopened?

    return (
        <div>
            <form>
                
            </form>
        </div>
    )
}

export default EnterScores