import { useState, useEffect } from 'react'

function AddRound() {

    //enter the basic info about the round - courses will have to be dynamically generated in a drop down bar based on courses user has played before
    //date and player will be automatic - need to set up login before getting to this

    return (
        <div>
            <form>
                
            </form>
        </div>
    )
}

export default AddRound